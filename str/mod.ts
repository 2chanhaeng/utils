/**
 * @module str
 *
 * Utilities for working with strings.
 *
 * @example
 * console.log(split(",")("foo,bar,baz")); // Logs: ["foo", "bar", "baz"]
 */
export { default as split } from "./split.ts";
export { default as trim } from "./trim.ts";
